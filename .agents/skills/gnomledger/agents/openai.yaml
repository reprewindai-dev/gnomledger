interface:
  display_name: "Gnomledger"
  short_description: "Repo-specific patterns and workflows for gnomledger"
  default_prompt: "Use the gnomledger repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true